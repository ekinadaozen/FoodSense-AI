# ============================================
# FoodSense AI - Docker Profile Configuration
# ============================================
# Overrides for running inside Docker Compose

spring:
  datasource:
    url: jdbc:oracle:thin:@oracle:1521/FREEPDB1
    username: foodsense
    password: foodsense_secret

  kafka:
    bootstrap-servers: kafka:29092

logging:
  level:
    root: INFO
    com.foodsense.ai: INFO
